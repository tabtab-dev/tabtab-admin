# 开发环境变量配置文件
# 仅在开发环境(dev server)时加载

# 网站标题 - 开发环境
VITE_APP_TITLE=TabTab Admin (Dev)

# 正式接口 BaseURL
VITE_API_BASE_URL=/api

# 是否使用 Mock 接口
# 设置为 true 时使用 Mock 接口，false 时使用正式接口
VITE_USE_MOCK=true

# Mock 接口 BaseURL
# 使用 Vite 内置 Mock 服务器时，设置为当前开发服务器地址
# 例如：http://localhost:5173/mock-api (Vite 默认端口)
VITE_MOCK_API_BASE_URL=/mock-api
