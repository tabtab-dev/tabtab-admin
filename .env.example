# ===========================================
# TabTab Admin 环境变量模板
# ===========================================
# 复制此文件为 .env.local 或 .env.development.local 进行本地配置
# ===========================================

# 应用标题 - 用于浏览器标签页标题
VITE_APP_TITLE=TabTab Admin

# API 基础路径
# 开发环境可设置为代理路径，生产环境设置为实际 API 地址
VITE_API_BASE_URL=/api

# 是否启用 Mock 数据（仅开发环境有效）
# 设置为 true 时，将使用 mock 数据而非真实 API
VITE_USE_MOCK=true
